<UserControl x:Class="MacropadConfigurator.Views.MacropadButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MacropadConfigurator.Views"
             xmlns:m="clr-namespace:MacropadConfigurator.Models"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200"
             d:DataContext="{d:DesignInstance Type=m:MacropadButton, IsDesignTimeCreatable=False}">
    <StackPanel>
        <TextBlock Text="Button" FontSize="24"/>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Text" Width="75" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Text}" MaxLength="5" Width="100"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Key" Width="75" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Key}" MaxLength="5" Width="100"/>
        </StackPanel>
    </StackPanel>
</UserControl>
