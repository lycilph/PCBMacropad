<mah:MetroWindow x:Class="MacropadConfigurator.Views.ShellWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:dia="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
                 xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:local="clr-namespace:MacropadConfigurator.Views"
                 xmlns:behavior="clr-namespace:MacropadConfigurator.Behaviors"
                 xmlns:vm="clr-namespace:MacropadConfigurator.ViewModels" 
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance Type=vm:ShellViewModel, IsDesignTimeCreatable=False}"
                 Title="Macropad Configurator" 
                 Height="700" 
                 Width="600"
                 WindowStartupLocation="CenterScreen"
                 ResizeMode="CanMinimize"
                 dia:DialogParticipation.Register="{Binding}">
    <i:Interaction.Behaviors>
        <behavior:WindowLifecycleBehavior/>
    </i:Interaction.Behaviors>

    <Window.InputBindings>
        <KeyBinding Key="F1" Command="{Binding ToggleSettingsCommand}"/>
    </Window.InputBindings>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands ShowSeparators="false">
            <Button Content="{icon:Lucide Kind=ArrowBigUp}"
                    ToolTip="Upload configuration to Macropad"
                    Command="{Binding UploadConfigurationCommand}"/>
            <Button Content="{icon:Lucide Kind=ArrowBigDown}"
                    ToolTip="Download configuration from Macropad"
                    Command="{Binding DownloadConfigurationCommand}"/>
            <Button Content="{icon:Lucide Kind=RotateCcw}"
                    ToolTip="Reset configuration on Macropad"
                    Command="{Binding ResetConfigurationCommand}"/>
            <Button Content="{icon:Lucide Kind=Settings}"
                    Command="{Binding ToggleSettingsCommand}"
                    ToolTip="Settings (F1)"/>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <mah:MetroWindow.Flyouts>
        <mah:FlyoutsControl>
            <mah:Flyout Header="Settings" 
                        Theme="Adapt"
                        Position="Right" 
                        Width="250"
                        IsOpen="{Binding SettingsViewModel.IsOpen}"
                        CloseCommand="{Binding ToggleSettingsCommand}">
                <ContentControl Content="{Binding SettingsViewModel}"/>
            </mah:Flyout>
        </mah:FlyoutsControl>
    </mah:MetroWindow.Flyouts>

    <Grid>
        <mah:TransitioningContentControl Content="{Binding Content}"
                                         Transition="LeftReplace"/>

        <Rectangle Fill="Gray"
                   Opacity="0.65"
                   Visibility="{Binding IsOverlayVisible, Converter={StaticResource bool_to_visibilty_converter}}">
            <Rectangle.InputBindings>
                <MouseBinding MouseAction="LeftClick"
                  Command="{Binding ToggleSettingsCommand}"/>
            </Rectangle.InputBindings>
        </Rectangle>

        <mah:ProgressRing IsActive="{Binding IsSpinnerVisible}"/>
    </Grid>
</mah:MetroWindow>
