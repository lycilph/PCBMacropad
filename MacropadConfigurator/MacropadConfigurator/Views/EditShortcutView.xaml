<UserControl x:Class="MacropadConfigurator.Views.EditShortcutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ae="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:local="clr-namespace:MacropadConfigurator"
             xmlns:vm="clr-namespace:MacropadConfigurator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:EditShortcutViewModel, IsDesignTimeCreatable=False}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal">
            <TextBlock Text="Name" Width="75" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Shortcut.Name}" Width="250"/>
            <TextBlock Text="Text" Width="75" Margin="20 0 0 0" VerticalAlignment="Center" ToolTip="Text shown on macropad (5 chars max)"/>
            <TextBox Text="{Binding Shortcut.Text}" MaxLength="5" Width="100"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0 5">
            <TextBlock Text="Key" Width="75" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Shortcut.Key}" Width="250"/>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0 5">
            <TextBlock Text="Modifiers" Width="75" VerticalAlignment="Center"/>
            <CheckBox Content="Shift" Width="75" IsChecked="{Binding Shift}"/>
            <CheckBox Content="Control" Width="75" IsChecked="{Binding Control}"/>
            <CheckBox Content="Alt" Width="75" IsChecked="{Binding Alt}"/>
        </StackPanel>

        <ae:TextEditor Grid.Row="3"
                       Margin="0 10"
                       SyntaxHighlighting="C#"
                       FontFamily="Consolas"
                       FontSize="10pt"
                       WordWrap="True"
                       BorderThickness="1"
                       BorderBrush="Black"
                       Document="{Binding Document}"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Save"
                    Width="75"
                    IsDefault="True"/>
            <Button Content="Cancel"
                    Width="75"
                    IsCancel="True"
                    Margin="10 0 0 0"
                    Command="{Binding BackCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
