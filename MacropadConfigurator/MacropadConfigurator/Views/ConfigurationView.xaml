<UserControl x:Class="MacropadConfigurator.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:MacropadConfigurator"
             xmlns:behavior="clr-namespace:MacropadConfigurator.Behaviors"
             xmlns:vm="clr-namespace:MacropadConfigurator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:ConfigurationViewModel, IsDesignTimeCreatable=False}">
    <i:Interaction.Behaviors>
        <behavior:LoadedBehavior/>
    </i:Interaction.Behaviors>

    <Grid Margin="20 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="2"
                   Text="Configuration"
                   FontSize="36"
                   Margin="0 0 0 10"/>

        <Button Grid.Row="0"
                Grid.Column="2"
                Content="{icon:Lucide Kind=Pencil}"
                Style="{DynamicResource MahApps.Styles.Button.Circle}"
                HorizontalAlignment="Right"
                Width="36"
                Height="36"/>

        <ListBox Grid.Row="1"
                 Grid.Column="0"
                 ItemsSource="{Binding Layers}"
                 SelectedItem="{Binding SelectedLayer}"
                 DisplayMemberPath="Name"/>

        <ListBox Grid.Row="1"
                 Grid.Column="1"
                 Margin="10 0"
                 ItemsSource="{Binding SelectedLayer.Buttons}"
                 SelectedItem="{Binding SelectedButton}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" Columns="3"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

        <TextBlock Grid.Row="1"
                   Grid.Column="2"
                   Text="Button setup"/>

        <StackPanel Grid.Row="2"
                    Grid.Column="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Save"
                    Width="75"
                    IsEnabled="False"/>
            <Button Grid.Column="2"
                    Content="Cancel"
                    Width="75"
                    Margin="10 0 0 0"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</UserControl>
