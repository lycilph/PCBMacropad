<UserControl x:Class="MacropadConfigurator.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:MacropadConfigurator"
             xmlns:behavior="clr-namespace:MacropadConfigurator.Behaviors"
             xmlns:vm="clr-namespace:MacropadConfigurator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:ConfigurationViewModel, IsDesignTimeCreatable=False}">
    <i:Interaction.Behaviors>
        <behavior:LoadedBehavior/>
    </i:Interaction.Behaviors>

    <Grid Margin="20 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="2"
                   Text="Configuration"
                   FontSize="36"
                   Margin="0 0 0 10"/>

        <Button Grid.Row="0"
                Grid.Column="1"
                Content="{icon:Lucide Kind=Pencil}"
                Style="{DynamicResource MahApps.Styles.Button.Circle}"
                HorizontalAlignment="Right"
                Width="36"
                Height="36"/>

        <StackPanel Grid.Row="1"
                    Grid.Column="0"
                    Orientation="Horizontal">
            <TextBlock Text="Layers"
                       VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Layers}"
                      SelectedItem="{Binding SelectedLayer}"
                      DisplayMemberPath="Name"
                      Margin="10 0 0 0"/>
            <CheckBox Content="Enabled"
                      IsChecked="{Binding SelectedLayer.IsEnabled}"
                      Margin="10 0 0 0"/>
        </StackPanel>

        <ListBox Grid.Row="2"
                 Grid.Column="0"
                 Margin="0 10 0 0"
                 ItemsSource="{Binding SelectedLayer.Buttons}"
                 SelectedItem="{Binding SelectedButton}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" Columns="3"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="5 0">
                        <StackPanel>
                            <TextBlock Text="{Binding Text}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Key, Converter={StaticResource key_converter}}"/>
                            <TextBlock Text="{Binding Modifier, Converter={StaticResource key_converter}}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ContentControl Grid.Row="2"
                        Grid.Column="1"
                        Content="{Binding SelectedButton}"/>

        <StackPanel Grid.Row="3"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Save"
                    Width="75"
                    IsEnabled="False"/>
            <Button Grid.Column="2"
                    Content="Cancel"
                    Width="75"
                    Margin="10 0 0 0"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</UserControl>
