<UserControl x:Class="MacropadConfigurator.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MacropadConfigurator"
             xmlns:behavior="clr-namespace:MacropadConfigurator.Behaviors"
             xmlns:vm="clr-namespace:MacropadConfigurator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0"
                TabStripPlacement="Left">
            <TabItem Header="Shortcuts"
                 Content="{Binding ShortcutsViewModel}"/>

            <TabItem Header="Configuration"
                 Content="{Binding ConfigurationViewModel}"/>
        </TabControl>

        <Border Grid.Row="1"
            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
            BorderThickness="1"
            Margin="2"
            Padding="2"
            CornerRadius="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl behavior:ScrollViewerBehavior.AutoScroll="True"
                              ItemsSource="{Binding Log}"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
