<UserControl x:Class="MacropadConfigurator.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:MacropadConfigurator.Views"
             xmlns:behaviors="clr-namespace:MacropadConfigurator.Behaviors"
             xmlns:vm="clr-namespace:MacropadConfigurator.ViewModels" 
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="10 10 10 0">
            <TextBlock Text="Layer:" 
                       FontSize="24"
                       VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Layers}"
                      SelectedItem="{Binding SelectedLayer}"
                      DisplayMemberPath="Name"
                      Margin="10 0 0 0"
                      FontSize="20"/>
            <mah:ToggleSwitch Content="Enabled"
                              Margin="10 0 0 0"/>
            <Button Content="{icon:Lucide Kind=Pencil}"
                    HorizontalAlignment="Right"
                    Style="{DynamicResource MahApps.Styles.Button.Circle}"/>
        </StackPanel>

        <ItemsControl Grid.Row="1"
                      ItemsSource="{Binding SelectedLayer.Shortcuts}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" Columns="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <Border Grid.Row="2"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                BorderThickness="1"
                Margin="2"
                Padding="2"
                CornerRadius="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl behaviors:ScrollViewerBehavior.AutoScroll="True"
                              ItemsSource="{Binding Log}"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
