<Application x:Class="MacropadConfigurator.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             xmlns:local="clr-namespace:MacropadConfigurator"
             xmlns:vm="clr-namespace:MacropadConfigurator.ViewModels"
             xmlns:v="clr-namespace:MacropadConfigurator.Views"
             xmlns:m="clr-namespace:MacropadConfigurator.Models"
             xmlns:c="clr-namespace:MacropadConfigurator.Converters"
             mc:Ignorable="d"
             Startup="ApplicationStartup"
             Exit="ApplicationExit">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- MahApps.Metro resource dictionaries. Make sure that all file names are Case Sensitive! -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <!-- Theme setting -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Steel.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <tb:TaskbarIcon x:Key="NotifyIcon"
                            d:DataContext="{d:DesignInstance Type=vm:NotifyIconViewModel, IsDesignTimeCreatable=False}"
                            IconSource="/Assets/ApplicationIcon.ico"
                            ToolTipText="Macropad Hotkeys"
                            DoubleClickCommand="{Binding ShowWindowCommand}">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Show Config" Command="{Binding ShowWindowCommand}"/>
                        <Separator />
                        <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}"/>
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
            
            <!-- Effects -->
            <DropShadowEffect x:Key="DropShadow"
                              BlurRadius="0"
                              ShadowDepth="5"
                              Direction="-45"
                              Color="{DynamicResource MahApps.Colors.ThemeForeground}"
                              Opacity="0.4" />
            
            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="bool_to_visibilty_converter"/>
            <c:KeyConverter x:Key="key_converter"/>
            <c:StringToBoolConverter x:Key="string_to_bool_converter"/>

            <!-- Datatemplates -->
            <DataTemplate DataType="{x:Type vm:MainViewModel}">
                <v:MainView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <v:SettingsView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type m:Shortcut}">
                <v:ShortcutView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:EditShortcutViewModel}">
                <v:EditShortcutView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:EditMasterScriptViewModel}">
                <v:EditMasterScriptView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
